{
  "name": "swds-server2",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "private": true,
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "start": "tsc && concurrently \"tsc -w --incremental\" \"nodemon dist\"",
    "start-docker": "tsc && node dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
